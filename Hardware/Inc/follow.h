/********************************************************
 * 文件: follow.h
 * 描述: 跟随模式模块头文件
 *       超声波锁定前方物体自动跟随功能
 *       搜索 → 跟随(保持20cm距离) → 丢失重搜
 ********************************************************/
#ifndef __FOLLOW_H
#define __FOLLOW_H

#include "sys.h"

/**
 * @brief  跟随模式主任务
 * @note   在主循环中周期调用，实现自动跟随功能
 *         状态0: 搜索目标(舵机扫描60°~120°范围)
 *         状态1: 锁定跟随(保持20cm目标距离)
 *         目标丢失后自动切回搜索状态
 * @warning 仅在 work_mode == MODE_FOLLOW 时调用
 *          依赖舵机(Servo)、超声波(SR04)、电机(Motor)模块
 */
void Follow_Task(void);

#endif
